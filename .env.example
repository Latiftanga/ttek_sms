DEBUG=1
SECRET_KEY=dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,*
DATABASE_URL=postgres://postgres:postgres@db:5432/school_db
POSTGRES_DB=school_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
REDIS_URL=redis://redis:6379/0

# SMS (console for development - prints to logs)
# Each school configures their own SMS provider in admin
SMS_BACKEND=console

# Email (console for development - prints to logs)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@tteksms.localhost