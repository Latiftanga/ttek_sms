# =============================================================================
# TTEK SMS - Railway.app Environment Variables
# =============================================================================
# Set these in the Railway dashboard under Variables tab for each service

# =============================================================================
# REQUIRED - Django Core Settings
# =============================================================================

# Set to 0 for production (disables debug mode)
DEBUG=0

# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=80)!mhw((l^titwb(&&!$iy@cil956pqo8@3zhx##bdi=(12)f

# Your Railway domain and custom domains (comma-separated)
ALLOWED_HOSTS=*.up.railway.app,yourdomain.com,*.yourdomain.com

# CSRF protection for forms (must include https://)
CSRF_TRUSTED_ORIGINS=https://*.up.railway.app,https://yourdomain.com,https://*.yourdomain.com

# =============================================================================
# DATABASE - Auto-injected by Railway when PostgreSQL is added
# =============================================================================
# DATABASE_URL is automatically provided by Railway PostgreSQL plugin
# Format: postgresql://user:password@host:port/database

# =============================================================================
# REDIS - Auto-injected by Railway when Redis is added
# =============================================================================
# REDIS_URL is automatically provided by Railway Redis plugin
# Used for Celery task broker and result backend

# =============================================================================
# SECURITY - Field Encryption
# =============================================================================
# Generate a new Fernet key for production:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=your-encryption-key-here

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tagnatekent@gmail.com
EMAIL_HOST_PASSWORD=B7ye6z9h5@
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# OPTIONAL - Platform Payment Credentials (for schools without their own)
# =============================================================================
# PLATFORM_PAYSTACK_SECRET_KEY=
# PLATFORM_PAYSTACK_PUBLIC_KEY=

# =============================================================================
# RAILWAY SERVICE CONFIGURATION
# =============================================================================
# For the main web service (uses railway.json):
#   - Build command: (handled by Dockerfile)
#   - Start command: ./railway-start.sh
#
# For the Celery worker service:
#   - Build command: (handled by Dockerfile)
#   - Start command: ./railway-worker.sh
#
# For the Celery beat service:
#   - Build command: (handled by Dockerfile)
#   - Start command: ./railway-beat.sh
#
# All services should share the same environment variables above.
# Link DATABASE_URL and REDIS_URL from the PostgreSQL and Redis services.
